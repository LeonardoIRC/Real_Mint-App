{
  "name": "realmintgpt",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "cd landing && npm run dev",
    "build": "cd landing && npm install && npm run build",
    "preview": "cd landing && npm run preview"
  }
}

